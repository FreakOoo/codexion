TODO - codexion

- simulate actual compile time: compile() currently increments the
  counter and stamps last_compile instantly, so TTB can basically
  never be reached under a realistic value. Add a delay based on
  time_to_compile (TTC) - decide whether it happens before taking the
  sticks (thinking time) or while holding them (actual compiling).

- implement the debug phase (TTD) and refactor phase (TTR): both are
  read from stdin into time_to_debug/time_to_refactor but immediately
  cast to (void) in main.c and never used anywhere.

- implement dongle cooldown (DONGLE_COOLDOWN): read into
  dongle_cooldown but voided in main.c. t_stick already has
  last_use_timer for this but it's only ever initialized to 0, never
  read or updated after a stick is released.

- implement the scheduler: SCHEDULER is read but voided in main.c,
  and scheduler.c is just a stub (`void *scheduler(void *)`, unnamed
  param, no body doing anything). It's also not in the Makefile's
  SRCS, so it isn't even compiled right now.

- add input validation in read_config / main: only `n <= 0` is
  checked today. Negative or zero TTB/TTC/TTD/TTR/COMPILES/
  DONGLE_COOLDOWN would currently be accepted silently.

- double check held_sticks[2]/held_count on t_person: they're
  written in lock_sticks/unlock_sticks (coder.c) but never read
  anywhere - confirm whether something is meant to consume them
  (e.g. future debug output) or drop them if they're dead state.
